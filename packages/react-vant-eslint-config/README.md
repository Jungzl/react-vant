# Eslint Config of React-Vant

## Install

```shell
# with npm
npm add @react-vant/eslint-config -D

# with yarn
yarn add @react-vant/eslint-config -D

# with pnpm
pnpm add @react-vant/eslint-config -D
```

## Usage

```json
{
  "extends": ["@react-vant"]
}
```
